from study_session import startStudySession
from test import measure_decibels
from test import forever
import threading

def main():
    measure_decibels()

if __name__ == "__main__":
    """     # Create a thread for the parallel function
    parallel_thread = threading.Thread(target=forever)

    # Start the parallel thread
    parallel_thread.start()

    # Run the main function in the main thread """
    
    main()